{
  "name": "sklad-server",
  "version": "1.0.0",
  "description": "Lokální server pro mobilní skladový systém",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",

    "db:reset": "node scripts/run-sql.js sql/schema.sql",

    "db:seed:base": "node scripts/run-sql.js sql/seed-base.sql",
    "db:seed:demo": "node scripts/run-sql.js sql/seed-demo.sql",
    "db:seed": "npm run db:seed:base && npm run db:seed:demo",

    "db:setup": "npm run db:reset && npm run db:seed",
    "db:setup:base": "npm run db:reset && npm run db:seed:base"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.3",
    "helmet": "^7.1.0",
    "multer": "^2.1.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}